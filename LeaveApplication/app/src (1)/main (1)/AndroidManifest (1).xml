<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shashimishra.leaveapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/leaveappicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin_login" />
        <activity android:name=".Form_Activity" />
        <activity android:name=".FinalForm1" />
        <activity android:name=".FinalForm2" />
        <activity android:name=".RegistrationForm" />
        <activity android:name=".OTPActivity" />
        <activity android:name=".NavigationalActivity"></activity>
    </application>

</manifest>